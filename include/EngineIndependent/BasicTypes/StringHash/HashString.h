#pragma once
#include "StringHashMap.h"
namespace MI {
	class HashString {
		StringHashMap<
		static Initialize() 
		static StringHashMap<
		unsigned int hashid;

		//Get a hash value from the given string.
		int Hash(const char*);

		//Get
		int HashRegister(const char*);
	}
}